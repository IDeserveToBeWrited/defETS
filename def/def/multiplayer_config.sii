SiiNunit
{
multiplayer_config : .multiplayer.config {
    remote_blinker_speed: (0.4, 0.4)    # Min max time it takes for blinkers to change state (on / off)

    dangerous_objects[]: "bridge_boat"
}
}
